version: '3.4'

services:
  nginx-alexrudy:
    build: .
    image: docker.alexrudy.local/alexrudy-jekyll:latest 
    environment:
      - VIRTUAL_HOST=www.alexrudy.local
    ports:
      - "127.0.0.1:4000:4000"
    volumes:
      - .:/src/jekyll-site
